{
  "name": "zahnarzt-praxis",
  "private": true,
  "version": "1.0.0",
  "description": "Praxis an der Wertachbrücke – Webseite + Admin-Konsole (Demo)",
  "workspaces": [
    "website",
    "admin"
  ],
  "scripts": {
    "dev:website": "npm run dev -w website",
    "dev:admin": "npm run dev -w admin",
    "build": "npm run build -w website && npm run build -w admin"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}
